---
title: 'Lecture4_Notes'
publishDate: 2025-11-23
description: 'TODO'
tags:
  - TODO
language: 'English'
heroImage: { src: './default.jpg', color: '#D58388' }
---

So welcome back to lecture four. Today we're going to talk about optimization. It seems like the mic in the room doesn't have full clip so I'm just going to talk out loud hopefully everyone can hear me yes in the background okay good. As you recall last the last couple lectures we've been developing this idea of linear classifiers. First we talked about how we can use linear classifiers to solve this image classification problem and that we can view linear classifiers from these three different perspectives algebraic visual and geometric. In the last lecture we then also talked about how we can use loss functions to quantify our preferences over different values of the weights when we're using linear classifiers. In particular we talked about the softmax and the SVM loss functions which impose different preferences over the sets of weights that we're going to use in our linear classifiers. We also talked about the use of regularization that can also express some preference over which values of weights we prefer and not prefer where we have this intuition that regularization helps us maybe generalize beyond the training set and build classifiers that can that are simpler and thus generalize better to the test set. But at the end of last lecture we were left with this open question of how given some linear classification set up and given a loss function how do we actually go about finding a value for this weight matrix W that satisfies this loss how do we actually fit our linear classifier to the data that we have in our training set well that's going to be the topic of today's lecture. So broadly we're going broadly this question of finding weight matrices that minimize a loss function is this topic of optimization. The general framework here is that we have some loss function L of W that's going to input our weight matrix and output our scalar loss. In the last lecture we talked about the internal components of that loss but for the purpose of today we're mostly going to extract all those away and just think about the loss function as an abstract function that inputs the weight matrix and outputs the scalar value of the loss.

欢迎回到第四讲。今天我们将讨论优化问题。看起来房间里的麦克风没有完全夹好，所以我只能大声说话，希望每个人都能听到我。背景音里的回应很好。正如你们回忆的前几讲内容，我们一直在发展线性分类器的概念。首先我们讨论了如何使用线性分类器来解决图像分类问题，以及我们可以从代数、视觉和几何这三个不同视角来看待线性分类器。在上次讲座中，我们还讨论了如何利用损失函数来量化在使用线性分类器时对不同权重值的偏好。特别地，我们讨论了softmax和SVM损失函数，它们对我们在线性分类器中要使用的权重集合施加了不同的偏好。我们还讨论了正则化的使用，它也能表达我们对哪些权重值偏好或不偏好的倾向，我们有一个直觉就是正则化可能帮助我们超越训练集进行泛化，并构建更简单因此能更好地泛化到测试集的分类器。但在上次讲座结束时，我们留下了一个开放性问题：给定某个线性分类设置和损失函数，我们实际上该如何找到满足这个损失函数的权重矩阵W的值？我们实际上如何将线性分类器拟合到训练集中的数据？这将成为今天讲座的主题。总的来说，寻找最小化损失函数的权重矩阵这个问题就是优化这个主题。这里的通用框架是，我们有一个损失函数L(W)，它输入我们的权重矩阵并输出我们的标量损失。在上一讲中我们讨论了该损失的内部组成部分，但为了今天的目的，我们将大部分忽略这些内容，只把损失函数看作一个抽象函数，它输入权重矩阵并输出损失的标量值。

And now during the process of optimization, what we want to do is find this value of the weight matrix W star that minimizes the loss function. This is why this topic of general optimization is indeed very general. It's a very broad topic across all of mathematics and applied computational mathematics and things like that. There's no way we can cover all possible pieces of optimization in one lecture, so instead we're going to focus on the bits that are most salient for training that will become more salient for training large neural network models as we move forward in the semester. This is kind of the formal way that we write down an optimization problem, but we often think about it intuitively as traversing a very large high-dimensional landscape. I often think about this process of optimization as trying to walk towards the bottom of some beautiful landscape. The intuition here is that each point each XY point on the ground is a different value of our weight matrix W, and the height of that point on the ground is the value of the loss function L of W. During the process of optimization, we're going to be some little blindfolded person with no eyes because he has no eyes if you look. I've used it on the slide then you can see that there's no he doesn't actually know which direction where is exactly the bottom of this valley. Somehow what we need to do is despite having no eyes, we need to somehow explore around through this high dimensional landscape and find this point at the very bottom of this optimization landscape. That looks like a nice place to visit so maybe we'd like to get to the bottom somehow. The question is how do we actually get to the bottom? One thing you might hope so one kind of hope thing we might be able to do in some special situations is to just write down the answer right. For certain types of optimization problems like might arise in linear classification, if you're familiar linear regression, we might be able to just write down an explicit equation for the for the bottom of this objective.

现在在优化过程中，我们想要做的是找到这个最小化损失函数的权重矩阵W*的值。这就是为什么一般优化这个主题确实非常通用。这是一个跨越所有数学和应用计算数学等领域的非常广泛的主题。我们不可能在一堂课中涵盖优化的所有可能部分，因此我们将专注于对训练最突出的部分，随着学期推进，这些部分对训练大型神经网络模型将变得更加重要。这是我们写下优化问题的一种正式方式，但我们通常直观地将其理解为穿越一个非常大的高维景观。我经常将优化过程想象为试图走向某个美丽景观的底部。这里的直观理解是，地面上每个XY点代表我们权重矩阵W的不同值，而该点在地面上的高度是损失函数L(W)的值。在优化过程中，我们将成为一个被蒙住眼睛没有视力的人，因为如果你看幻灯片上的图示，会发现他实际上不知道这个山谷的确切底部在哪个方向。我们需要做的是尽管没有视力，我们仍需要以某种方式在这个高维景观中探索，并找到这个优化景观最底部的点。那看起来是个值得游览的好地方，所以我们可能想以某种方式到达底部。问题是我们实际上如何到达底部？你可能希望在某些特殊情况下能够做到的一件事就是直接写出答案。对于某些类型的优化问题，比如线性分类中可能出现的问题，如果你熟悉线性回归，我们可能能够直接为这个目标的底部写出一个明确的方程。

Landscape but in general that's not going to work so we need to find more so we're going to use iterative methods instead to try to iteratively improve our solutions and go towards the bottom of this objective landscape. So the first iterative algorithm that you might imagine to optimize an objective landscape, which by the way is a terrible idea but it's instructive to think about, is random search. So here what we might try to do is simply generate many many random values of the weight matrix and then evaluate their loss value of these different random matrices on the training set and just keep track of the overall lowest value of loss that we find over this random search procedure. And if we've abstracted away our loss function into a single function like we've been talking about, then such a procedure can be implemented in just a couple lines of Python. So here we're using random search to train some linear model on CIFAR-10 dataset. And after running this for some amount of time we actually maybe are able to get something like 15.5 percent accuracy which you know is not bad because this is a pretty stupid algorithm, pretty stupid optimization algorithm, but even random search is actually able to make some kind of non-trivial progress on this optimization problem. But of course on this data set state of the art is something like 95 percent so we've got a little bit of gap to close on the rest of this lecture. So then idea number two is actually what we will do in practice. So here we're going to try to follow the slope of this landscape downhill. So here we believe that even though this little man walking around the objective landscape doesn't have eyeballs, he can somehow feel with his feet around on the ground and feel which direction is sloping, which direction the ground is sloping in a local region around where he's currently standing.

但一般来说这是行不通的，所以我们需要寻找更多方法，因此我们将改用迭代方法，试图迭代地改进我们的解决方案，并朝着这个目标景观的底部前进。所以你可能想到的第一个优化目标景观的迭代算法——顺便说一句这是个糟糕的主意但思考它很有启发性——就是随机搜索。这里我们可能尝试做的只是简单地生成许多许多权重矩阵的随机值，然后在训练集上评估这些不同随机矩阵的损失值，并仅跟踪在这个随机搜索过程中发现的总体最低损失值。如果像我们讨论的那样将损失函数抽象为单一函数，那么这样的过程只需几行Python代码就能实现。这里我们使用随机搜索在CIFAR-10数据集上训练某个线性模型。运行一段时间后，我们实际上可能获得大约15.5%的准确率，这还不错，因为这是一个相当愚蠢的算法，相当愚蠢的优化算法，但即使是随机搜索实际上也能在这个优化问题上取得某种非平凡的进展。但当然在这个数据集上，最先进水平大约是95%，所以在本讲剩余部分我们还有一点差距需要弥补。那么第二个想法实际上是我们将在实践中采用的方法。这里我们将尝试沿着这个景观的坡度向下走。这里我们相信，尽管这个在目标景观中行走的小人没有眼球，但他能以某种方式用脚感受地面，感知哪个方向是倾斜的，在他当前站立位置的局部区域中地面朝哪个方向倾斜。

So given only this local information about how the objective landscape is changing in a local neighborhood of our current point, then our strategy is to simply step a little bit in the direction of greatest decrease and then we can repeat this over and over and hopefully this will lead us towards some lower point in the objective landscape. So this is a fairly simple straightforward algorithm, but it actually works quite well. So here to make this a little bit more formal, we recall that we need to talk about derivatives right because if you recall for a scalar value function that inputs a single scalar and outputs a single scalar, we can define the derivative which tells us the slope at any point in the domain of this function where the slope tells us for any point how if we change x by a little bit then how much is y going to change a little bit in correspondence. So that's the familiar gradient that's the familiar derivative that we have from single variable calculus and of course this extends very naturally to multiple dimensions as well. So if you'll recall for a vector-valued function that inputs a vector and outputs a scalar which is what we're talking about in this optimization setup, then we can define the gradient where the gradient at a point is now a vector telling us the direction of greatest increase in the function. So now the gradient tells us the direction of greatest increase and if you'll recall your vector calculus, you'll know that the magnitude of the gradient tells us the slope in the direction of greatest increase. And it's also kind of a nice fact that the direction we actually care about the direction of greatest decrease because we want to walk downhill this objective landscape and it turns out the direction of greatest decrease is opposite the direction of greatest increase which is maybe not obvious but it happens to be true. And then so we can simply step in the direction of the negative gradient.

因此，仅凭关于目标景观在当前点局部邻域内如何变化的这些局部信息，我们的策略就是简单地朝着最大下降方向迈出一小步，然后我们可以一遍又一遍地重复这个过程，希望这将引导我们走向目标景观中的某个较低点。所以这是一个相当简单直接的算法，但效果却相当不错。为了让这一点更正式一些，我们回顾一下需要讨论导数，因为如果你还记得对于输入单个标量并输出单个标量的标量值函数，我们可以定义导数，它告诉我们该函数定义域内任意点的斜率，其中斜率告诉我们对于任意点，如果我们稍微改变x，那么y会相应地改变多少。这就是我们从单变量微积分中熟悉的梯度，熟悉的导数，当然这也很自然地扩展到多个维度。所以如果你还记得对于输入向量并输出标量的向量值函数——这正是我们在这个优化设置中讨论的情况——那么我们可以定义梯度，其中某一点的梯度现在是一个向量，告诉我们函数最大增加的方向。所以现在梯度告诉我们最大增加的方向，如果你还记得向量微积分，你就会知道梯度的大小告诉我们最大增加方向上的斜率。还有一个很好的事实是，我们实际关心的是最大减少的方向，因为我们想在这个目标景观中下坡，而事实证明最大减少的方向与最大增加的方向相反，这可能不明显，但恰好是真实的。然后我们可以简单地朝着负梯度的方向迈出一步。

And this will lead us downhill down our objective landscape. So then the question is how might we actually go about computing these gradients for arbitrary vector input for arbitrary functions that input a vector and output a scalar. Well we can actually implement this limit definition of the gradient directly in software. So you could imagine that we have some current value of the weight matrix w here on the left shown as a big vector and now on the right we want to compute this gradient of the loss with respect to the weight matrix. And recall that the loss is a single scalar value so the gradient of the loss with respect to the weight matrix will be a vector that has the same shape as the weight matrix itself where each element each slot in that gradient vector will tell us the slope if we change one element of that corresponding part of the weight matrix by just a little bit. So we could just implement this limit definition of a gradient numerically. So given our function that computes the loss what we could do is compute the loss at our current w then we could perturb the value of w by increasing the first slot of the weight matrix by a small step each and now run that perturbed value of w through our loss function to compute a new loss and then apply the limit definition of the derivative to compute some approximation to the gradient to some approximation to the first slot of the gradient. And here remember that it's maybe rise over run so we look at the difference in loss value divided by the difference in amount in which we change that slot of the input weight matrix w so that gives us some approximation to the slope of that function along the coordinate axis of the first slot of the weight matrix. And of course we can repeat this procedure for then second dimension perturb the second dimension recompute a new value of the loss and then again apply limit definition of the derivative to get this numeric approximation to the second slot we can repeat for the third slot etcetera etcetera. So this is called the numeric gradient and it's a reasonable thing to think about.

这将引导我们下坡，沿着目标景观下降。那么问题是我们如何实际计算这些梯度，针对任意向量输入的任意函数，这些函数输入向量并输出标量。实际上我们可以直接在软件中实现这个梯度的极限定义。所以你可以想象，在左边我们有权重矩阵w的某个当前值，显示为一个大向量，现在在右边我们想要计算损失相对于权重矩阵的梯度。请记住损失是一个单一标量值，因此损失相对于权重矩阵的梯度将是一个与权重矩阵本身形状相同的向量，其中梯度向量中的每个元素每个槽位将告诉我们，如果我们稍微改变权重矩阵对应部分的一个元素，斜率是多少。所以我们可以数值上实现这个梯度的极限定义。给定我们计算损失的函数，我们可以做的是在当前w处计算损失，然后我们可以通过将权重矩阵的第一个槽位增加一小步来扰动w的值，然后通过我们的损失函数运行这个扰动的w值来计算新的损失，然后应用导数的极限定义来计算梯度第一个槽位的某种近似值。这里请记住，可能是上升量除以前进量，所以我们查看损失值的差异除以我们改变输入权重矩阵w该槽位量的差异，这给出了我们该函数沿着权重矩阵第一个槽位坐标轴斜率的某种近似值。当然我们可以为第二个维度重复这个过程，扰动第二个维度，重新计算损失的新值，然后再次应用导数的极限定义来获得第二个槽位的数值近似，我们可以为第三个槽位等重复这个过程。所以这被称为数值梯度，这是一个值得思考的合理方法。

But the problem is that it's very slow so in this particular example our weight matrix w has maybe 10 or 12 slots i don't remember exactly how many i wrote down so if this would actually not be super expensive to compute but as we move to larger neural network based systems our weight matrices will become very very large they might have thousands or millions or tens of millions or in the biggest cases perhaps even billions of learnable parameters and now in order to compute a gradient using this numeric approximation this limit definition we need to compute a separate forward pass of our loss function once for each slot in our gradient in our weight matrix so this will become very impractical as we move to very high dimensional optimization problems and it's also approximate because we were relying upon a finite differences approximation to this gradient to this full true end to this full true gradient so this numeric gradient not only is it slow but it's also not even computing the correct thing at all but it's still useful to think about in terms of what is actually going on when we try to compute gradients in these large scale systems so okay so now at this point you might have thought that this was kind of a stupid thing to have done right we all hope because we all should have taken some vector-valued calculus because i think i put that down as a prereq for the class and now you know that you could just write down the loss function as some equation and now what we want to do is derive the gradient and you could as the gradient of this loss function with respect to the weight matrix w and if you're familiar with all the proper rules of manipulating these uh vector or matrix equations then we can get some help from these guys and just write down an equation that helps that directly defines the value of this loss function um do you know who these guys are newton and liveness you know which one is which yes okay yeah i think they all look kind of people have looked kind of funny back in those days but uh now what we want to do is then hopefully we can then just use our knowledge of vector calculus to

但问题是这种方法非常慢。在这个特定例子中，我们的权重矩阵w可能有10或12个槽位，我不记得具体写了多少，所以计算起来实际上不会特别昂贵。但当我们转向更大的基于神经网络的系统时，我们的权重矩阵会变得非常非常大，它们可能有数千、数百万、数千万甚至最大的情况下可能有数十亿个可学习参数。现在为了使用这种数值近似来计算梯度，即这个极限定义，我们需要为权重矩阵梯度中的每个槽位分别计算一次损失函数的前向传递，因此当我们处理非常高维的优化问题时，这将变得非常不切实际。而且这也是近似的，因为我们依赖于对这个完整真实梯度的有限差分近似。所以这种数值梯度不仅速度慢，甚至根本没有计算正确的东西，但思考在大规模系统中尝试计算梯度时实际发生的情况仍然是有用的。那么现在，你可能已经觉得这样做有点愚蠢，对吧？我们都希望如此，因为我们都应该学过一些向量值微积分，因为我认为我把它列为课程的先决条件。现在你知道你可以把损失函数写成某个方程，而我们想要做的是推导梯度，你可以作为这个损失函数相对于权重矩阵w的梯度。如果你熟悉处理这些向量或矩阵方程的所有适当规则，那么我们可以从这些人那里得到一些帮助，直接写出一个定义这个损失函数值的方程。你知道这些人是谁吗？牛顿和莱布尼茨，你知道哪个是哪个吗？是的，好吧，我觉得他们看起来都挺有趣的，那时候的人看起来都有点滑稽。但现在我们想要做的是，希望我们可以利用我们的向量微积分知识来

Write down an expression that tells us directly what is an algebraic or analytic expression for the exact gradient as a function of the weight matrix w. So then what this looks like is back to our current picture we will have our current weight matrix w that will then somehow derive this expression for the derivative of the loss with respect to the weight matrix and that will allow us to compute this gradient the ldw in a single operation or a single pass that hopefully now it will no longer require a number of forward passes linear in the number of dimensions of the weight matrix. The details of exactly how we will derive these gradient expressions in practice will often rely on back propagation which we'll talk about in much more detail in lecture six. For the time being you can just hope to fall back on your knowledge of calculus but moving forward we'll use back propagation as a more structured algorithm to derive gradients for arbitrarily large and arbitrarily complex expressions. But for the linear classifiers we've considered so far hopefully you should be able to work it out on paper or if not then you can use back propagation even for those as we'll talk about. Now the summary up to this point is that we've decided upon this strategy of minimizing a loss function by using gradient information that is we're going to compute gradients at every point and then use that gradient to iteratively improve our estimate of where exactly we want to fall in the loss landscape. Now we've talked about two different methods of computing gradients that can be applicable to any sort of function that you might imagine in general. So we talked about this numeric gradient that is very simple to understand very intuitive very easy to implement but it's approximate and it's slow. So in practice we'll typically use the analytic gradient which will be exact and fast but because deriving analytic gradients requires you to actually do some calculus on paper sometimes it can be error-prone.

写出一个表达式，直接告诉我们精确梯度的代数或解析表达式，作为权重矩阵w的函数。那么这看起来就像是回到我们当前的图景，我们将有当前的权重矩阵w，然后以某种方式推导出损失相对于权重矩阵导数的表达式，这将允许我们在单次操作或单次传递中计算这个梯度ldw，希望现在不再需要与权重矩阵维度数量成线性关系的前向传递次数。在实践中我们如何推导这些梯度表达式的细节通常依赖于反向传播，我们将在第六讲中更详细地讨论。目前你可以依靠你的微积分知识，但接下来我们将使用反向传播作为更结构化的算法来推导任意大和任意复杂表达式的梯度。但对于我们迄今为止考虑的线性分类器，希望你应该能够在纸上计算出来，如果不能，那么你也可以使用反向传播，正如我们将要讨论的那样。到目前为止的总结是，我们已经决定使用梯度信息来最小化损失函数的策略，即我们将在每个点计算梯度，然后使用该梯度迭代改进我们对在损失景观中确切位置的估计。现在我们已经讨论了两种不同的计算梯度的方法，这些方法可以适用于你可能想象到的任何类型的函数。所以我们讨论了这个数值梯度，它非常容易理解、非常直观、非常容易实现，但它是近似的且速度慢。因此在实践中我们通常使用解析梯度，它将是精确且快速的，但因为推导解析梯度需要你在纸上进行一些微积分计算，有时可能会容易出错。

A mistake and make a mistake in your computation make a mistake in your derivation which definitely happens when you're trying to think about how to compute gradients of these large complicated expressions. So in practice what we'll often do is actually use both. So in practice what we'll typically do is use the numeric gradient as a debugging tool to make sure that we properly implemented the analytic gradient. So here we would typically then write our code in a way that's agnostic to the number of dimensions in the weight matrix so and then we can test the analytic version of our gradient of our gradient computation on a very low dimensional version of the problem where computing a numeric gradient might be feasible and then we can check that we're computing the same approximately the same value between this numeric approximation and between this analytic gradient that will derive on paper. So this is always a really good idea that you should almost all you should always do this whenever you're implementing your own gradient code and this is just a really good way to make sure that you don't have any stupid mistakes when you've implemented gradients. So we will use this strategy on the homework assignments and you'll see starting on assignment two that again will be released later today you will be implementing your own gradient computations for linear classifiers and now to debug those linear classifiers we've written this grand check function that will come internally compute the numeric gradient and then compare it to the analytic gradient that you guys will derive. This is a very useful strategy and it's applicable beyond homework assignments as well if you look in the pytorch documentation you'll also find that pytorch provides a function called grad check that does something very similar. So if you ever find yourself writing your own gradient computations for code out there in the wild I would strongly encourage you to use this grad check function provided by pytorch to make sure that you properly implemented your gradients and if you look all further in the documentation you'll also find a grant grad check that helps you properly implement second derivatives which is also a thing that you sometimes need to do in pytorch. I think we'll see a couple examples of that later in the course but surprisingly.

在计算中犯错，在推导中犯错，这在思考如何计算这些大型复杂表达式的梯度时肯定会发生。所以在实践中，我们通常会同时使用两种方法。在实践中，我们通常会将数值梯度作为调试工具，以确保我们正确实现了解析梯度。因此，我们通常会以与权重矩阵维度无关的方式编写代码，然后我们可以在问题的非常低维版本上测试梯度计算的解析版本，其中计算数值梯度可能是可行的，然后我们可以检查数值近似和我们在纸上推导的解析梯度之间是否计算出大致相同的值。这总是一个非常好的想法，在实现自己的梯度代码时应该始终这样做，这是确保在实现梯度时没有任何愚蠢错误的好方法。我们将在作业中使用这种策略，从今天稍后发布的第二个作业开始，你将看到自己实现线性分类器的梯度计算，为了调试这些线性分类器，我们编写了这个梯度检查函数，它将在内部计算数值梯度，然后与你们推导的解析梯度进行比较。这是一个非常有用的策略，也适用于作业之外，如果你查看PyTorch文档，你还会发现PyTorch提供了一个名为grad check的函数，它做了非常类似的事情。所以如果你在野外编写自己的梯度计算代码，我强烈建议你使用PyTorch提供的这个grad check函数，以确保你正确实现了梯度，如果你进一步查看文档，你还会发现一个grad grad check，它帮助你正确实现二阶导数，这也是你在PyTorch中有时需要做的事情。我想我们将在课程后期看到几个这样的例子，但令人惊讶的是。

There's no grad grad grad check because it turns out that because of the way high torch implements gradients once you've got grad check to pass and grad grad check to pass then grad star check will definitely work for any any number of grads that you want to take. So again if you ever find yourself implementing gradient code out there in the wild you should probably use these tools to make sure that they're implemented properly. So now that we have some sense of how to compute gradients it finally brings us to this algorithm of gradient descent. So recall that we had talked about this this way this strategy for optimizing the loss function we're going to start somewhere and then deal with our b to see what is the local gradient direction and then step in that direction and then repeat. It turns out that this algorithm actually is very simple to implement right it fits on four lines of code here. So this very simple algorithm of gradient descent, we're going to initialize the weight somewhere we're going to loop for some fixed number of iterations then we will compute the gradient at our current point and then step in the negative gradient direction after we've computed our data in some way. This algorithm you'll notice has a couple hyper parameters and unlike the k n hyper parameters that you probably in fact won't play around with too much in practice these hyper parameters involved in gradient descent will become some of your best friends or maybe worst enemies as you're trying to build neural network-based systems. So here when we're talking about gradient descent hyper parameters that come to mind one would be your method of initializing the weights it turns out that intuitively we want to initialize the weights to be some random values but it turns out that for various applications the exact mechanism the exact distribution from which you draw those random values turns out to matter quite a lot in downstream applications. So we will talk in a little bit later lectures about the proper way to initialize weights when we're training neural network systems. The second hyperparameter is the number of steps we will need to run this out we need to run this algorithm in finite time because we actually want our homework.

没有grad grad grad检查，因为事实证明，由于PyTorch实现梯度的方式，一旦你通过了grad检查和grad grad检查，那么grad star检查肯定适用于你想要进行的任何数量的梯度计算。所以再次强调，如果你在野外编写梯度代码，你应该使用这些工具来确保它们正确实现。既然我们对如何计算梯度有了一些了解，这最终将我们带到了梯度下降算法。回想一下我们讨论过的优化损失函数的策略：我们从某个地方开始，然后处理我们的b来查看局部梯度方向，然后朝那个方向迈步，然后重复。事实证明，这个算法实际上非常容易实现，它只需要四行代码。这个非常简单的梯度下降算法，我们将在某个地方初始化权重，然后循环固定次数的迭代，然后计算当前点的梯度，然后在我们以某种方式计算数据后朝负梯度方向迈步。你会注意到这个算法有几个超参数，与k近邻的超参数不同（实际上你在实践中可能不会过多调整它们），梯度下降中涉及的这些超参数在你尝试构建基于神经网络的系统时将成为你最好的朋友或最坏的敌人。所以当我们谈论梯度下降超参数时，想到的第一个是你的权重初始化方法。直观上我们想要将权重初始化为一些随机值，但事实证明对于各种应用，你从中抽取这些随机值的精确机制、精确分布在下游应用中非常重要。所以我们将在稍后的讲座中讨论训练神经网络系统时初始化权重的正确方法。第二个超参数是我们需要运行这个算法的步数，我们需要在有限时间内运行这个算法，因为我们实际上想要我们的作业。

We want our models to converge before the homework is due, so we need to run this algorithm for some finite number of steps. There are many different stopping criteria you might imagine using in gradient descent, but the most common one in deep learning is simply to run for some fixed number of iterations. This hyperparameter is usually constrained by your computational budget and how long you're willing to wait for your models to train. The third hyperparameter here is the learning rate. When we take this step in the negative gradient, we actually need to say how much we actually trust this gradient and how big of a step we want to take. The local gradient tells us the direction of greatest decrease for our function, but we need to actually decide how much we want to step in that direction of greatest decrease. The hyperparameter controlling this step size is usually called the learning rate because it controls how fast your network learns, where higher learning rates means we're taking larger steps and maybe you might hope that your algorithm would converge faster. Lower learning rates are maybe less prone to numeric explosion but will take longer to converge and will take longer to learn. So these three hyperparameters will be some of your friends throughout the rest of the semester. You can again look at this sort of pictorially. We can imagine this algorithm of gradient descent by looking at one of these heat maps. Here the x and y axis maybe show us the values of two dimensions of the weight matrix and the color at each point represents the height of the objective function. We can imagine starting at some white point here, we compute our negative gradient which is now the direction of greatest decrease in the loss function, then we'll take some small step size along this gradient direction and iteratively repeat as we step downhill.

我们希望模型在作业截止前收敛，因此我们需要在有限步数内运行该算法。梯度下降中有许多不同的停止标准可供选择，但在深度学习中，最常见的方法就是运行固定的迭代次数。这个超参数通常受限于你的计算预算以及你愿意等待模型训练多长时间。第三个超参数是学习率。当我们沿着负梯度方向迈步时，实际上需要确定我们对该梯度的信任程度以及我们希望迈出多大的步长。局部梯度告诉我们函数下降最快的方向，但我们需要实际决定在这个最快下降方向上迈出多大的步长。控制这个步长的超参数通常称为学习率，因为它控制着网络学习的速度，较高的学习率意味着我们迈出更大的步长，可能希望算法收敛更快。较低的学习率可能不太容易出现数值爆炸，但需要更长时间才能收敛和学习。所以这三个超参数将在本学期剩下的时间里成为你的伙伴。你可以再次以图示方式来看待这个问题。我们可以通过观察其中一个热图来想象梯度下降算法。这里的x轴和y轴可能显示权重矩阵两个维度的值，每个点的颜色代表目标函数的高度。我们可以想象从某个白点开始，计算负梯度（即损失函数下降最快的方向），然后沿着这个梯度方向迈出一小步，并在下坡过程中迭代重复。

So as we actually run this algorithm it might look something like this where we will start at some point in the blue region where blue is this is kind of a maybe taco shell shaped or bowl shape objective landscape and we'll start at some high point in the blue region and then by iteratively following the gradient direction you can see that this algorithm very quickly converges to this red region of low loss at the middle of the tackle. And what's kind of just by looking at this at this time at this animation you can recognize a couple interesting features of the gradient descent algorithm. One is that it does not go straight to the bottom because this is sort of a taco bowl shaped landscape the gradient direction is actually at an angle to the direct to going straight to the bottom so it does not go straight to the bottom instead it kind of arcs and curves around to the side before coming back into the bottom of the objective landscape. You can also notice that it starts out going very fast oh no maybe I didn't set up the powerpoint to loop properly oh well well because of the way that we formulated this gradient descent by having a multiplicative learning rate on top of the steps on top of the gradient what this means is that when the gradient has a large magnitude we'll end up taking larger steps and as we get as we approach this bottom of the bowl the objective landscape will flatten out and the magnitudes of our gradients will become small and now our step sizes will also naturally become smaller. It's kind of nice this is a kind of nice way to parameterize gradient descent because it lets the algorithm somehow naturally slow down as we approach flat regions or as we pro maybe approach minima of the objective function. This version of gradient descent that we've some so far been talking about is sometimes called batch gradient descent or full batch gradient descent because as you recall our loss function is this giant sum over the individual loss functions for all the examples in our training data set where l i is where x i y i is one of the training pairs in our data set and l i is the loss function how well our classifier is doing on that one individual example and now our faux loss function on the training set as you'll recall is a sum over all parts over all the the individual elements or training examples in.

当我们实际运行这个算法时，它可能看起来像这样：我们从蓝色区域的某个点开始，蓝色区域可能是一个类似墨西哥卷饼壳或碗状的目标地形，我们从蓝色区域的某个高点开始，然后通过迭代跟随梯度方向，你可以看到这个算法非常快速地收敛到中间的红色低损失区域。通过观察这个动画，你可以识别出梯度下降算法的几个有趣特征。一是它不会直接到达底部，因为这是一个类似墨西哥卷饼碗状的地形，梯度方向实际上与直接到达底部的方向成一定角度，所以它不会直接到达底部，而是在返回目标地形底部之前以弧线和曲线的方式绕到侧面。你还可以注意到它一开始移动得非常快，哦不，也许我没有正确设置PowerPoint循环播放，好吧，由于我们通过在学习率上乘以梯度步长来制定这个梯度下降，这意味着当梯度幅度较大时，我们会采取更大的步长，而当我们接近碗底时，目标地形会变平，我们的梯度幅度会变小，现在我们的步长也会自然变小。这种方式很好，这是一种很好的参数化梯度下降的方法，因为它让算法在我们接近平坦区域或可能接近目标函数最小值时自然地减速。我们到目前为止讨论的这个版本的梯度下降有时被称为批量梯度下降或全批量梯度下降，因为正如你所记得的，我们的损失函数是对训练数据集中所有样本的单个损失函数的一个巨大求和，其中l_i是当x_i y_i是我们数据集中的一个训练对时，l_i是损失函数，表示我们的分类器在那个单个样本上的表现如何，而我们现在训练集上的损失函数，正如你所记得的，是对所有部分、所有单个元素或训练样本的求和。

Our dataset, and correspondingly because the gradient is a linear operation, you can see that the gradient is again a sum of the gradients over all of the individual examples in our training set. The problem here is that this sum can become very expensive as your dataset becomes large. If n is something like a million or 10 million or a billion, if you're lucky enough to have a very large dataset, then simply computing the value of this loss function will involve a loop over tens of thousands or millions or billions of training samples. So in order to compute just a single step of gradient descent, you'd be waiting a very long time to loop through your entire dataset. In practice, this will therefore not be very feasible for training on the large datasets that we want in order to get good performance. In practice, we often use a variant of gradient descent called stochastic gradient descent or SGD. Here the idea is that rather than computing a sum over the full training dataset, we will approximate this loss function and approximate the gradient by drawing small subsamples of our full training dataset. The typical sizes here in these small subsamples are called mini batches, and the typical sizes of these will often be something like 32 or 64 or 128 elements at a time. Then what we will basically do is modify our algorithm so that in each iteration, rather than computing the loss on the full dataset, we'll sample a small mini batch of items from the dataset and then compute our gradient and our loss using only this small sampled set of elements. This version of stochastic gradient descent is very common and is what we'll almost always use in practice. Once we move from full batch to stochastic gradient descent, we introduce a couple more hyperparameters into the system. We still have our old friends of weight initialization, number of steps, and learning rate. We've also introduced batch size as a new hyperparameter, that is, when we're computing these mini batches, how many elements should be in each mini.

我们的数据集，相应地，由于梯度是线性运算，你可以看到梯度又是训练集中所有单个样本梯度的总和。这里的问题是，随着数据集变大，这个求和会变得非常昂贵。如果n是百万、千万甚至十亿的量级，如果你足够幸运拥有非常大的数据集，那么仅仅计算这个损失函数的值就需要遍历数万、数百万甚至数十亿的训练样本。因此，为了计算梯度下降的单个步骤，你将需要等待很长时间来遍历整个数据集。实际上，这对于我们为了获得良好性能而需要训练的大型数据集来说不太可行。在实践中，我们经常使用一种称为随机梯度下降（SGD）的梯度下降变体。这里的想法是，我们不计算整个训练数据集的总和，而是通过从完整训练数据集中抽取小子样本来近似这个损失函数和梯度。这些小子样本的典型大小被称为小批量，其典型大小通常是每次32、64或128个元素。然后，我们基本上会修改算法，使得在每次迭代中，我们不计算整个数据集的损失，而是从数据集中抽取一个小批量样本，然后仅使用这个小子样本集来计算梯度和损失。这种随机梯度下降版本非常常见，在实践中我们几乎总是使用它。一旦我们从全批量转向随机梯度下降，我们就在系统中引入了更多的超参数。我们仍然有我们的老朋友：权重初始化、步数和学习率。我们还引入了批量大小作为一个新的超参数，即当我们计算这些小批量时，每个小批量中应该包含多少个元素。

Match thankfully it turns out empirically that this one tends not to be too sensitive of a hyper parameter and the general heuristic here is just make the mini batch size as large as you can until you run out of GPU memory. If you happen to have access to multiple GPUs or multiple machines, it turns out that you can distribute this and have very very large batches that are distributed over multiple GPUs or multiple machines. It turns out sort of this is all empirically speaking that for many neural network systems the exact batch size you use doesn't matter too much as long as you properly account for other things in the system. So we'll talk more about those details later in the class but the general rule of thumb here is don't worry too much about the batch size as a hyper parameter instead just try to make it as big as you can fit. Then we've introduced another hyper parameter here which is the method by which we're going to sample our training data at every iteration. For classification problems like we've been talking about so far this tends not to matter too much. On the homework assignments what we'll do is just draw the data at random for each iteration. Another common strategy would be to shuffle the data set at the beginning, we're going to shuffle the data set and then march through that shuffled data set in order and then shuffle the data set again and then march through it again in order. For classification problems like we've been considering the latter is more common but I think it tends not to matter too much for your final results. But if you think about other types of problems like structured prediction problems or maybe triplet matching problems or ranking problems then the way in which you iterate over your data can sometimes be an important type of parameter but thankfully that's not the case for many image classification problems. So you might be wondering why this is called stochastic gradient descent it's kind of a funny name. The reason for that is that we now think of our loss function probabilistically so we then sometimes think of a data.

幸运的是，经验表明这个超参数往往不太敏感，这里的一般启发式方法就是让迷你批次大小尽可能大，直到耗尽GPU内存。如果你碰巧可以使用多个GPU或多台机器，事实证明你可以将其分布处理，在多个GPU或多台机器上分布非常大的批次。从经验上来说，对于许多神经网络系统，只要你适当考虑系统中的其他因素，你使用的确切批次大小并不太重要。所以我们将在课程后面详细讨论这些细节，但这里的一般经验法则是不要过于担心批次大小这个超参数，而是尽量让它尽可能大。然后我们在这里引入了另一个超参数，即我们每次迭代采样训练数据的方法。对于像我们到目前为止讨论的分类问题，这往往不太重要。在作业中，我们将为每次迭代随机抽取数据。另一种常见策略是在开始时打乱数据集，我们将打乱数据集，然后按顺序遍历那个打乱后的数据集，然后再次打乱数据集，再次按顺序遍历。对于像我们一直在考虑的分类问题，后者更常见，但我认为这对你的最终结果往往不太重要。但如果你考虑其他类型的问题，比如结构化预测问题，或者可能是三元组匹配问题或排序问题，那么你迭代数据的方式有时可能是一个重要的参数类型，但幸运的是，对于许多图像分类问题来说并非如此。所以你可能会想知道为什么这被称为随机梯度下降，这是一个有趣的名字。原因是我们现在从概率角度考虑我们的损失函数，所以我们有时会考虑数据。

Of an underlying probability, probably joint probability distribution over the data x and the labels y from which our training sum was sampled. When we think about our data as having been sampled from some underlying probability distribution, then we can write down our loss function as this expectation as we take an expectation over all possible samples from some true underlying data distribution. Then the form of the loss function that we've seen thus far, which is an average over the independent loss functions on all of the samples from our dataset, is then a Monte Carlo estimation to this full expectation over the full probability distribution. And then clearly when you think about loss functions in this way, then we have a choice in how many samples we want to take in order to approximate this full expectation for computing loss function. This notion of thinking probabilistically is therefore what is named stochastic, where this term stochastic comes from in the name stochastic gradient descent. And of course, the gradient is much the same, so then we can approximate the gradient as well by taking a small Monte Carlo estimate of samples over this whole distribution. We have an interactive web demo up here that you can, if it's still on stanford.edu, I guess I need to fix that, that you can play around with that will let you play with linear classifiers and using gradient descent to train different types of linear classifiers interactively in a web browser. And I would encourage you to check this out to gain some intuition about how you can change the loss function, change the values of the weights, change the step size and sort of see interactively how changing all these hyperparameters will affect the ways in which decision boundaries shift around and affect the training of linear classifiers. So it turns out that this actually works now. At this point, you know enough to go and implement the linear classifier portion on assignment two. But it turns out that this simple algorithm of stochastic gradient descent, even though it's actually quite effective, there are some potential problems with stochastic gradient descent.

关于一个基础概率，可能是数据x和标签y的联合概率分布，我们的训练样本就是从这个分布中抽取的。当我们把数据看作是从某个基础概率分布中抽样得到时，我们可以将损失函数写成这种期望形式，即我们对来自某个真实基础数据分布的所有可能样本取期望。那么，我们迄今为止看到的损失函数形式，即对数据集中所有样本的独立损失函数取平均值，就是对这个完整概率分布上全期望的蒙特卡洛估计。很明显，当你以这种方式思考损失函数时，我们就有选择权来决定要取多少样本来近似这个用于计算损失函数的全期望。这种概率性思考的概念因此被称为"随机"，这就是随机梯度下降中"随机"一词的来源。当然，梯度也是类似的，因此我们也可以通过在整个分布上取少量样本的蒙特卡洛估计来近似梯度。我们这里有一个交互式网络演示，如果它还在stanford.edu上，我想我需要修复一下，你可以用它来在线性分类器上进行实验，并在网络浏览器中交互式地使用梯度下降来训练不同类型的线性分类器。我鼓励你去看看这个演示，以获得一些直观认识，了解如何改变损失函数、改变权重值、改变步长，并交互式地观察改变所有这些超参数会如何影响决策边界的移动方式以及线性分类器的训练效果。所以事实证明这个方法现在确实有效。在这一点上，你已经掌握了足够的知识去完成作业二中的线性分类器部分。但事实证明，这个简单的随机梯度下降算法，尽管实际上相当有效，也存在一些潜在问题。

And let's think about a couple situations where this basic version of stochastic gradient descent might run us into trouble. Well one problem is what might happen if our loss landscape looks something like this. So here we're showing a contour plot of a loss landscape. This loss landscape is therefore a kind of very exaggerated taco shell type landscape where it's changing very quickly in one direction and then changing very slowly in another direction. And the question is what might happen if we apply a stochastic gradient descent or full batch gradient descent on an objective landscape that has this shape. Well one problem is that our steps might oscillate around. If our step size is too large then we kind of have a trade-off we need to set our step size. If our step size is too large then when we compute the gradient, we might overshoot in the fast moving dimension and then have to correct our overshoot and come back in the other direction. And this can cause a kind of zigzagging pattern as we then zigzag towards the minimum that can cause us to take many more steps than we might otherwise have needed to take if we were somehow have been able to use a smarter optimization algorithm. And we kind of have a trade-off here because we could have maybe avoided this by setting the step size very very small. But if we set the step size very very small that might prevent this overshoot zigzagging pattern but it would then cause the algorithm to just converge very very slowly. And now you're kind of in a tough spot in this situation. If you've got an objective landscape that's very fast moving in one direction and very slow moving in the other direction then you're kind of screwed no matter how you set the step size. If you set it too big you're going to overshoot the fast moving dimension. If you set it too small then you'll make no progress towards the goal. And this problem is technically sometimes referred to as the problem having a high condition number, which you can estimate numerically by looking at the ratio between the singular values of the

让我们思考几种情况，这种基本版本的随机梯度下降可能会让我们陷入困境。一个问题是我们损失函数的地形看起来像这样会发生什么。这里我们展示了一个损失函数地形的等高线图。这个损失函数地形是一种非常夸张的墨西哥卷饼壳类型的地形，它在一个方向上变化非常快，而在另一个方向上变化非常缓慢。问题是，如果我们在具有这种形状的目标地形上应用随机梯度下降或全批次梯度下降会发生什么。一个问题是我们可能会在周围振荡。如果我们的步长太大，那么我们就需要权衡设置步长。如果步长太大，那么当我们计算梯度时，我们可能会在快速移动的维度上过度调整，然后不得不纠正过度调整并朝另一个方向返回。这可能导致一种锯齿状模式，当我们锯齿状地向最小值移动时，这可能导致我们比使用更智能的优化算法需要更多的步骤。我们在这里需要权衡，因为我们可以通过将步长设置得非常非常小来避免这种情况。但如果我们把步长设置得非常非常小，可能会防止这种过度调整的锯齿状模式，但会导致算法收敛得非常非常慢。在这种情况下，你处于一个困难的境地。如果你有一个目标地形，在一个方向上移动非常快，在另一个方向上移动非常慢，那么无论你如何设置步长，你都会陷入困境。如果你设置得太大，你会在快速移动的维度上过度调整。如果你设置得太小，那么你向目标前进的进度会很慢。这个问题在技术上有时被称为具有高条件数的问题，你可以通过查看奇异值的比率来数值估计

Hessian matrix at any point. We don't need to go into detail here about that, but this is one potential problem for using this vanilla stochastic gradient descent. Another potential problem with stochastic gradient descent is functions that exhibit local minimum or saddle points. A local minima is a point on the function where the function has zero gradient, but it's not actually the bottom of the function. Here we show a one-dimensional example where we kind of go down and then there's a local minimum. In order to escape that local minimum, we would need to go over some hill or something. You can imagine that if we were using this basic stochastic gradient descent algorithm on some function that had a local minimum of this form, then we would get stuck in this local minimum because the gradient is zero there. So our step size would converge to zero and we might end up stopping at this local minimum and be unable to escape. Since the gradient there is zero as well, we can have a similar problem. There's another problem that I think is much more common in high dimensional optimization, which is the notion of a saddle point. Here a saddle point is a point where in one direction the function is increasing and in another direction the function is decreasing. As it's called a saddle point, it looks kind of like a saddle on a horse. The problem here is that at the tip of this saddle, the gradient is also zero. If we were to imagine trying to optimize a function that had a saddle point, you can imagine we might get stuck in the saddle point similarly as we might get stuck in a local minimum because the gradient at the saddle point is identically zero. The problem in both these situations is that because they have zero gradient, we might get stuck. Just to point out, this notion of a saddle point actually I think becomes much more common in high dimensions because if our optimization landscape is very high dimensional, a saddle point is basically a point where in one direction the objective function is increasing and in another direction the objective function is decreasing.

在任何点的海森矩阵。我们不需要在这里详细讨论这个问题，但这是使用这种普通随机梯度下降的一个潜在问题。随机梯度下降的另一个潜在问题是存在局部最小值或鞍点的函数。局部最小值是函数上梯度为零的点，但它实际上并不是函数的最低点。这里我们展示了一个一维示例，我们向下走然后遇到一个局部最小值。为了逃离这个局部最小值，我们需要越过某个山丘之类的东西。你可以想象，如果我们在具有这种形式局部最小值的函数上使用这种基本随机梯度下降算法，那么我们会被困在这个局部最小值中，因为那里的梯度为零。所以我们的步长会收敛到零，我们可能会在这个局部最小值处停止并且无法逃脱。由于那里的梯度也为零，我们可能会遇到类似的问题。我认为在高维优化中更常见的是另一个问题，即鞍点的概念。鞍点是指在一个方向上函数递增，在另一个方向上函数递减的点。正如其名鞍点，它看起来有点像马鞍。这里的问题是在这个鞍点的尖端，梯度也为零。如果我们想象尝试优化一个具有鞍点的函数，你可以想象我们可能会像被困在局部最小值中一样被困在鞍点中，因为鞍点处的梯度同样为零。这两种情况的问题在于，由于它们具有零梯度，我们可能会被困住。需要指出的是，我认为鞍点的概念实际上在高维中变得更加常见，因为如果我们的优化地形是高维的，鞍点基本上就是一个目标函数在一个方向上递增而在另一个方向上递减的点。

If we have an objective landscape with something like 10,000 dimensions or a million dimensions, then it seems very plausible then actually very frequent that at many points in the objective landscape we might be increasing in some of those dimensions and decreasing in other of those dimensions. So the intuition is that somehow perhaps saddle points are maybe a big problem in high dimensional optimization. Another potential problem with stochastic gradient descent is that stochastic part because we are computing our gradients using only a small estimate of our full data set. Those gradients are noisy and the gradients that we're using to make our updates at any step of the algorithm may not correlate very well with the true direction that we want to go to in order to go to the bottom. On the right we're showing an animation here that simulates this problem where we're running a gradient descent on this same bowl shape objective landscape as before but now we're adding some amount of noise to the gradients that we're computing when running the algorithm. And you can see that in contrast to the version of stochastic gradient descent previously that was kind of marching down towards the bottom, instead we see that once our gradients are noisy then our algorithm is somehow kind of meandering around this objective landscape and kind of taking its time towards getting toward to the bottom. And you might maybe if maybe you might hope that if you wait long enough this will kind of average out and get us towards the bottom but it's still not exactly obvious what the trade-offs there are. So I think it's a bit of a problem potentially that our gradients in stochastic gradient descent are not exact that they are stochastic approximations to the true gradient that we want to descend upon. To overcome all these problems therefore it's actually not so common to use this very vanilla form of stochastic gradient descent. We'll often use slightly smarter versions of stochastic gradient descent when training our when training neural networks in practice. The most common of these is called SGD plus momentum. So here on the left we're showing our old friend the stochastic gradient descent algorithm and now I've for the remainder of the slides.

如果我们有一个具有10,000维或百万维的目标地形，那么似乎非常合理且实际上非常频繁的是，在目标地形的许多点上，我们可能在某些维度上递增，而在其他维度上递减。因此直觉上，鞍点可能在高维优化中是一个大问题。随机梯度下降的另一个潜在问题是其随机性部分，因为我们仅使用完整数据集的一小部分估计来计算梯度。这些梯度是有噪声的，我们在算法任何步骤中用于更新的梯度可能与我们想要到达底部的真实方向相关性不佳。在右侧我们展示了一个动画来模拟这个问题，我们在之前相同的碗状目标地形上运行梯度下降，但现在在运行算法时给计算的梯度添加了一些噪声。你可以看到，与之前那种向底部行进的随机梯度下降版本相比，一旦我们的梯度变得有噪声，我们的算法就会在目标地形上徘徊，花费时间慢慢接近底部。你可能会希望如果等待足够长的时间，这会平均化并将我们带向底部，但权衡关系仍然不太明确。所以我认为随机梯度下降中梯度不精确是一个潜在问题，它们是我们想要下降的真实梯度的随机近似。因此为了克服所有这些问题，实际上很少使用这种非常基础的随机梯度下降形式。在实践中训练神经网络时，我们通常会使用稍微更智能的随机梯度下降版本。其中最常见的是带动量的SGD。所以在左侧我们展示了我们的老朋友随机梯度下降算法，现在我已经为剩余的幻灯片

I've cut out the weight initialization step because that's going to be common to all algorithms. On the left with our familiar friend stochastic gradient descent, you see that at every iteration we're simply stepping in the direction of the gradient as has been computed with our mini batch of examples. Now with SGD plus momentum on the right, we imagine a kind of physical intuition here of actually a ball rolling down this high dimensional surface. So now at every point we imagine integrating the gradients over time to compute some kind of a velocity vector for this ball rolling down a hill. Now at every point in time we will then update this velocity vector by taking some weighted combination of the current value of the gradient and this historical velocity, this historical moving average of gradients. Then when we take our gradient step we will not use the true gradient direction, instead we will step in this direction of this computed velocity vector that we've computed over the whole course of training. You should imagine this as like a marble rolling downhill that now as it kind of speeds downhill it picks up some velocity and now it continues moving in that direction even if the local gradient is not directly aligned with its direction of motion. Concretely we implement this on the right by introducing a new scalar hyperparameter called row which is something like you can think of as like a friction or a decay rate. Now at every point in time we're going to keep track of two things: we're going to keep track of our positions x t and as well as our velocity vector vt. Now at every point in time we're going to update our velocity vector by first we'll first decay it by multiplying by this scalar friction value and then we'll add back in the value of the gradient computed at the current point. Now when we make our step we'll step according to this velocity vector. As kind of a technical note depending on which papers or which textbooks you'll read, you'll sometimes see to

我省略了权重初始化步骤，因为这对所有算法都是通用的。在左侧，我们熟悉的老朋友随机梯度下降算法中，你可以看到在每次迭代中，我们只是沿着通过小批量样本计算的梯度方向前进。现在在右侧的带动量SGD中，我们想象一种物理直观感受，就像一个球沿着这个高维表面滚动。所以在每个点上，我们想象随时间整合梯度来计算这个滚下山坡的球的某种速度向量。在每个时间点，我们将通过取当前梯度值和这个历史速度、这个历史梯度移动平均值的某种加权组合来更新这个速度向量。然后当我们进行梯度步进时，我们不会使用真实的梯度方向，而是会沿着我们在整个训练过程中计算出的这个计算速度向量的方向前进。你应该把这想象成一个滚下山坡的弹珠，当它加速下山时会获得一些速度，现在即使局部梯度与其运动方向不完全一致，它也会继续沿着那个方向移动。具体来说，我们在右侧通过引入一个名为ρ的新标量超参数来实现这一点，你可以把它想象成摩擦力或衰减率。现在在每个时间点，我们将跟踪两件事：我们将跟踪我们的位置x_t以及我们的速度向量v_t。现在在每个时间点，我们将通过首先乘以这个标量摩擦值来衰减它，然后再加上在当前点计算的梯度值来更新我们的速度向量。现在当我们进行步进时，我们将根据这个速度向量前进。作为一个技术说明，根据你阅读的不同论文或教科书，你有时会看到

These two slightly different formulations of SGD plus momentum that have slightly different looking equations, but as an exercise to the reader you can go back and work this out at home and show to yourself that these two formulations are actually equivalent. In the sense that they'll actually visit the same sequence of weight values no matter which formulation you're using, so it's something of an implementation detail as to which of these two formulations you choose. But I wanted you to be aware that you might see different formulations depending on which source you're reading. Now once we have this notion of SGD plus momentum, we can think about how it helps to solve all three of these problems that we pointed out with the basic SGD algorithm. Recall that we talked about one potential problem with SGD as local minimum where because the gradient was zero at the bottom of a local minima then there's zero gradient, so if we step according to the true gradient we'd be stuck forever. But now once we augment the stochastic gradient descent algorithm with momentum, now because you should imagine this as like a ball rolling down the hill that as we descend from the top and come down to the local minimum, then even as we pass that local minimum our point might still have some velocity that can help carry us up the other side and to hopefully escape from that local minimum by using our velocity to kind of power through it in some way. And a similar intuition applies for saddle points. Again once we start building up velocity by rolling downhill, then we can hopefully roll right over those saddle points and continue descending down along a different direction. Momentum can also help us overcome a little bit this problem of poor conditioning because what you can imagine this velocity vector is kind of like a weighted moving average, an exponentially weighted moving average all the gradients that we see during training. So now if we see gradients, if we start off in this oscillate, if we see this oscillatory behavior during training, then you could imagine that our velocity vector would help smooth that.

这两种略有不同的带动量SGD公式虽然看起来方程形式稍有差异，但作为读者的练习，你可以回家仔细研究并证明这两种公式实际上是等价的。从它们无论使用哪种公式都会访问相同的权重值序列这个意义上来说，所以选择这两种公式中的哪一种更多是一个实现细节。但我想让你知道，根据阅读的资料不同，你可能会看到不同的公式表述。现在我们有了带动量SGD的概念，可以考虑它如何帮助我们解决基本SGD算法中指出的这三个问题。回想我们讨论过SGD的一个潜在问题是局部最小值，因为在局部最小值底部梯度为零，所以如果我们按照真实梯度步进，就会永远卡住。但现在一旦我们用动量增强随机梯度下降算法，因为你应该把这想象成一个滚下山坡的球，当我们从顶部下降到达局部最小值时，即使我们经过那个局部最小值，我们的点可能仍然有一些速度，可以帮助我们冲上另一侧，并有望逃离那个局部最小值，通过以某种方式利用我们的速度冲过去。类似的直观理解也适用于鞍点。再次，一旦我们通过滚下山坡开始积累速度，那么我们有望直接滚过那些鞍点，并沿着不同方向继续下降。动量还可以帮助我们稍微克服条件数差的问题，因为你可以把这个速度向量想象成一种加权移动平均，即我们在训练期间看到的所有梯度的指数加权移动平均。所以现在如果我们看到梯度，如果我们从这种振荡开始，如果我们在训练期间看到这种振荡行为，那么你可以想象我们的速度向量会有助于平滑这种。

Out and the quick oscillations along the y axis here would then be averaged out to give us maybe hopefully relatively low velocity or relatively low velocity in that direction. And then maybe we might even accelerate our velocity along the horizontal direction and help us smooth out or deal better with these objective landscapes with high condition number. Gradient descent with momentum can also help us with this problem of stochasticity. So here on the right we're showing again this same animation of adding using gradient descent. So the black line is showing gradient descent where we're adding some amount of stochasticity, some amount of random noise to the gradient at every point. And as we saw before the black line is kind of meandering all over the objective landscape due to the effect of the noise on the gradients. In the blue we're instead showing the same sequence of gradient updates but instead using stochastic gradient descent with momentum. And here we can see that by adding momentum to the algorithm it's somehow able to smooth out the noise and take a more direct path towards the bottom of the objective landscape. Another way to think about what the momentum update is doing is somehow with this little diagram here where we imagine that at every point in time we're sitting at this red dot. And then at this red dot we can compute we have this green vector which is some direction giving us our velocity which is something like our historical averages of the gradients we've seen during training. And now we will also then compute this red vector which tells us the instantaneous gradient at this point. And then we will average them to get this new velocity in blue which is the actual step direction that we'll use to update our weight matrix. So the intuition here is that we're combining the gradient at the current point with this historical average of gradients and this helps us smooth out our optimization procedure. There's another version of momentum that you'll sometimes see called Nesterov momentum that has something of a similar intuition but it kind of interleaves these steps in a slightly different order.

y轴上的快速振荡会被平均掉，从而可能让我们在该方向上获得相对较低的速度。然后我们甚至可能沿着水平方向加速我们的速度，帮助我们更好地平滑或处理这些高条件数的目标地形。带动量的梯度下降也能帮助我们解决随机性问题。在右侧我们再次展示这个使用梯度下降的相同动画。黑线显示的是梯度下降，我们在每个点的梯度上都添加了一定程度的随机性，一定量的随机噪声。正如我们之前看到的，由于梯度上的噪声影响，黑线在目标地形上蜿蜒曲折。蓝色线显示的是相同的梯度更新序列，但使用的是带动量的随机梯度下降。这里我们可以看到，通过给算法添加动量，它能够以某种方式平滑噪声，并采取更直接的路径到达目标地形的底部。另一种理解动量更新作用的方式是通过这个小图表，我们想象在每个时间点我们都位于这个红点上。在这个红点上，我们可以计算这个绿色向量，它给出了我们的速度方向，类似于我们在训练期间看到的梯度的历史平均值。现在我们还会计算这个红色向量，它告诉我们该点的瞬时梯度。然后我们将它们平均得到这个蓝色的新速度，这就是我们将用于更新权重矩阵的实际步进方向。所以这里的直观理解是，我们将当前点的梯度与这个梯度的历史平均值结合起来，这有助于平滑我们的优化过程。还有另一种动量版本，你有时会看到叫做Nesterov动量，它有着类似的直观理解，但以稍微不同的顺序交错这些步骤。

With Nesterov momentum, what we do is we kind of imagine a bit of a look ahead. So now we're still starting at this red point at every iteration. We still have this historical green vector of velocities that is our moving average somehow of all the directions we've seen during training. But now the difference between Nesterov and traditional stochastic gradient descent plus momentum is that with Nesterov momentum, we imagine looking ahead into the future and computing what would the gradient direction have been if I would have stepped according to the velocity vector. So we kind of look ahead in the direction of the velocity vector and compute the new value of the gradient in that look ahead. And then when we take the actual step, our actual step is then a linear combination of this velocity direction as well as this direction that was computed via this lookahead step. And this Nesterov momentum then somehow has a similar effect as gradient descent with momentum. It just integrates the past and the present versions of the gradient in a slightly different way. So you'll sometimes see people use these two different formulations for optimizing things. If we want to look at that, we can write this down mathematically and look at Nesterov momentum in the following way. So here we see that we still keep a running tally of our velocity vector as well as our position vector. But now when we update our velocity vector, we compute the gradient at this look ahead point. So this look ahead point is now x t plus rho v t, which is now what the gradient would have been as we step in the direction of the velocity vector. Then we compute the gradient there, and then our velocity is then a combination of our old velocity plus the velocity at the look ahead point. And then we update the position x using this velocity computed.

使用Nesterov动量时，我们所做的是进行某种程度的前瞻。所以在每次迭代中，我们仍然从这个红点开始。我们仍然有这个历史性的绿色速度向量，它在某种程度上是我们在训练期间看到的所有方向的移动平均值。但现在Nesterov和传统带动量随机梯度下降之间的区别在于，使用Nesterov动量时，我们想象向前展望未来，并计算如果我按照速度向量步进，梯度方向会是什么。所以我们沿着速度向量的方向向前看，并在那个前瞻位置计算梯度的新值。然后当我们实际步进时，我们的实际步进是这个速度方向以及通过这个前瞻步骤计算出的方向的线性组合。这个Nesterov动量然后以某种方式具有与带动量梯度下降类似的效果。它只是以稍微不同的方式整合了梯度的过去和现在版本。所以你有时会看到人们使用这两种不同的公式来优化事物。如果我们想看看这个，我们可以用数学方式写下来，并通过以下方式来看Nesterov动量。这里我们看到我们仍然保持对速度向量以及位置向量的运行记录。但现在当我们更新速度向量时，我们在这个前瞻点计算梯度。所以这个前瞻点现在是x t加上ρ v t，这就是当我们沿着速度向量方向步进时梯度会是什么。然后我们在那里计算梯度，然后我们的速度就是我们旧速度加上前瞻点速度的组合。然后我们使用计算出的速度更新位置x。

This is kind of an awkward formulation unfortunately for an optimization algorithm. Usually it's more convenient to implement optimization algorithms when they take when they only depend upon the current point and the gradient at the current point which this Nesterov function doesn't seem to be right because it has this funny structure of forcing us to compute gradients to look ahead. It doesn't fit nicely into this API of objective of optimization functions that we expect. So the question is is our velocity vector in the direction of the gradient or is our velocity vector in the direction of the negative gradient and the other choice is whether your velocity vector incorporates the learning rate or do you incorporate the learning rate after you do the velocity vector. So these are all kind of equivalent they all kind of you can imagine pulling these different factors out so they're all kind of equivalent and it doesn't really matter which one you use. In this version of Nesterov what we're doing is we're having the velocity be actually the negative direction of the gradient and we're rolling the learning rate alpha into the velocity vector as well. So then when we take this step in the direction of velocity vector it's actually stepping in the negative direction of the gradient. So then we said that we saw that this Nesterov update has a slightly awkward formulation it doesn't quite fit into the API of functions that we might want to write. So it turns out there's a simple change of variables we can use that I don't want to work through here but just for you to be aware it's kind of fun to work through this on paper and see with this change of variables actually Nesterov momentum can be rewritten in terms of only the current gradient and the current position which is kind of cool given that it actually has this intuition of a look ahead. So this is a track that you'll sometimes see. Now we can return to this our friend we can return to this simple example and now compare our SGD algorithm in black with SGD plus momentum in blue and Nesterov momentum in green and we can see that they both accelerate the training process quite a lot that because we're building up this velocity vector over time they're somehow able to accelerate in quick and quickly moving regions of the objective landscape.

不幸的是，对于优化算法来说，这是一种有些尴尬的表述。通常，当优化算法仅依赖于当前点和当前点的梯度时，实现起来更为方便，而这个Nesterov函数似乎不太对，因为它具有这种奇怪的结构，迫使我们计算梯度来向前看。它不太适合我们期望的优化函数目标API。所以问题是我们的速度向量是沿着梯度方向还是负梯度方向，另一个选择是速度向量是否包含学习率，或者是在计算速度向量之后才加入学习率。这些在某种程度上都是等价的，你可以想象把这些不同的因素提取出来，所以它们都是等价的，使用哪一个并不重要。在这个Nesterov版本中，我们所做的是让速度实际上是梯度的负方向，并且我们将学习率α也纳入速度向量中。所以当我们沿着速度向量方向步进时，实际上是在沿着梯度的负方向步进。所以我们说我们看到这个Nesterov更新有一个稍微尴尬的表述，它不太适合我们可能想要编写的函数API。事实证明，有一个简单的变量变换我们可以使用，我不想在这里详细推导，但只是让你知道，在纸上推导这个很有趣，通过这个变量变换，实际上Nesterov动量可以仅用当前梯度和当前位置来重写，这很酷，因为它实际上具有这种前瞻的直觉。所以这是你有时会看到的一种方法。现在我们可以回到这个简单的例子，比较黑色的SGD算法、蓝色的带动量SGD和绿色的Nesterov动量，我们可以看到它们都大大加速了训练过程，因为随着时间的推移我们建立了这个速度向量，它们能够以某种方式在目标地形的快速移动区域中加速。

And then slow down and sort of converge towards the end. You can see another feature here that's very common with momentum methods is that they tend to overshoot at the bottom. So because we're building up this velocity over time either with traditional or Nesterov momentum, then we've actually got some non-trivial velocity by the time that we get to the bottom of the bowl. Yeah question, the question is why is the Nesterov line green with a black overlay? I think it's because I'm bad at JavaScript and I didn't implement this in a good way. But yeah, that's not significant, it should just be a nice clean green line. So but then you can see that both Nesterov and traditional momentum kind of have a slightly similar character in that they build up the velocity over time and then overshoot at the bottom and can kind of come back. So any questions about these momentum-based methods? Well these are okay. So then these are actually very commonly used in practice to train not only linear models but also a lot of deep learning models as well. So then moving ahead, there's another kind of category of optimization functions that we sometimes see which is this notion of sometimes called adaptive learning rates and a classic example here is the Adagrad algorithm. Here we similarly want to find some way to overcome some of these problems that we talked about with SGD, but now with Adagrad we're going to overcome it in a slightly different way. So rather than tracking this whole historical average of gradient values, instead we're going to keep track of a historical average of squares of gradient values. And now when we make our step, then you can see up here in this algorithm we're keeping a running average, a running sum of all the element-wise squared values of the gradients that we see over time. And now when we make our step, then we end up dividing by the square root of this historical sum. So this seems like kind of a funny thing to do and it's not exactly clear what's going on here. So to motivate this Adagrad optimization algorithm, we can think back to this.

然后减速并在接近结束时逐渐收敛。你可以看到动量方法另一个常见的特征，就是它们往往在底部过冲。因为随着时间的推移，无论是传统动量还是Nesterov动量，我们都在积累速度，所以当我们到达碗底时，实际上已经有了一些不可忽视的速度。是的，问题是为什么Nesterov线是绿色带黑色覆盖？我认为这是因为我不擅长JavaScript，没有很好地实现这个。但这不重要，它应该只是一条漂亮的干净绿线。所以你可以看到Nesterov和传统动量都有点相似的特征，它们随着时间的推移积累速度，然后在底部过冲并能够返回。那么关于这些基于动量的方法有什么问题吗？这些还不错。所以这些实际上在实践中非常常用，不仅用于训练线性模型，也用于许多深度学习模型。那么接下来，还有另一类我们有时会看到的优化函数，这个概念有时被称为自适应学习率，一个经典的例子是Adagrad算法。在这里，我们同样想找到某种方法来克服我们讨论过的SGD的一些问题，但现在使用Adagrad，我们将以稍微不同的方式克服它。所以不是跟踪整个梯度值的历史平均值，而是跟踪梯度值平方的历史平均值。现在当我们进行步进时，你可以在这个算法中看到，我们保持一个运行平均值，即我们在整个过程中看到的梯度所有元素平方值的运行总和。现在当我们进行步进时，我们最终会除以这个历史总和的平方根。所以这看起来像是做了一件有趣的事情，而且不太清楚这里发生了什么。所以为了激发这个Adagrad优化算法，我们可以回想一下这个。

Taco shell-shaped landscape and you can think about what happened what does the autograd algorithm do in this when we have this objective landscape well what's going on is remember the problem here was that we had in this objective landscape we had one direction where the gradient was changing very fast so then if the gradient was changing very fast then this anagram will then end up dividing by a large value so it will help damp down progress in directions where the gradient is changing very fast and now in the other direction where the gradient is changing very slowly then add a ground will end up dividing by a large value so oh right no it will not dividing by a small value see this stuff is confusing right you ought to work it through for yourself um then you end up dividing by a small value so it has the effect of accelerating motion along directions where the gradient is very small so then we can hope that this can help us overcome these kind of ill-conditioned types of objective landscapes um but now there's maybe a problem with autograph so what might happen if we run this adoge algorithm for a very long time well if we run at a grad for a very long time you can see that our grad squared will just continue accumulating continue accumulating um because squares are always positive so then this grad square will just grow and grow and grow over the course of optimization which means that we're going to be dividing by a larger and larger and a larger thing so which means that that has this effect of um sort of effectively decaying the step size or decaying the learning rate um continually over the course of learning and it's possible that this um grad squared might end up getting too big and might cause us to stop making progress in the direction we want to make progress and so we could end up stopping somewhere that's before we get to the bottom of the objective landscape so so um to overcome this problem people sometimes use uh a variant or sometimes people don't use at a grad directly um a way to fix this problem is this uh another another optimization algorithm called.

Taco壳形的地形，你可以思考一下发生了什么，当我们有这种目标地形时，autograd算法会做什么？情况是这样的：记住这里的问题是，在这个目标地形中，我们有一个方向梯度变化非常快，所以如果梯度变化非常快，那么这个算法最终会除以一个大值，所以它有助于抑制在梯度变化很快的方向上的进展。而在另一个梯度变化很慢的方向上，那么adagrad最终会除以一个大值，哦不对，它不会除以一个小值，这些东西很令人困惑，你应该自己推导一下，嗯，然后你最终除以一个小值，所以它具有在梯度很小的方向上加速运动的效果。因此我们可以希望这能帮助我们克服这种病态类型的目标地形。但现在autograd可能有一个问题，如果我们长时间运行这个adagrad算法会发生什么？如果我们长时间运行adagrad，你可以看到我们的梯度平方会继续累积，继续累积，嗯，因为平方总是正的，所以这个梯度平方在优化过程中会不断增长，这意味着我们将除以一个越来越大、越来越大的东西，这意味着它具有有效衰减步长或衰减学习率的效果，在学习过程中持续衰减。有可能这个梯度平方最终变得太大，可能导致我们在想要取得进展的方向上停止进展，所以我们可能最终停在目标地形底部的某个地方。因此，嗯，为了克服这个问题，人们有时使用一个变体，或者有时人们不直接使用adagrad，嗯，解决这个问题的方法是这种，嗯，另一个，另一个优化算法叫做。

RMS prop that you can kind of think of as a leaky version of Adagrad. Just as you recall that in stochastic gradient descent plus momentum we had some kind of friction coefficient that was decaying the velocities at every iteration before we compute them. Now with RMS prop it looks very much like adding like Adagrad with this extra friction term that's going to decay our running average of square gradients. Here the hope is that by adding some kind of friction to our Adagrad algorithm then it will cause it to make better progress and not slow down all the time over the course of training. It's kind of instructive to then look back at the same example of this optimization problem now comparing SGD in black, SGD plus momentum in blue and RMS prop in red. Here we can see that they have fairly sorry about the red on orange color choice maybe that was not so great but what hopefully you might be able to make out if you've got really good color vision is that the blue and the red have sort of qualitatively different behavior in how they navigate this landscape. When we use this momentum-based method in blue you can see that it tends to overshoot and then come back whereas when we use this second order this adaptive learning rates idea from Adagrad or RMS prop you can see that it sort of arrests progress along the fast moving direction while simultaneously accelerating progress along slow moving direction which helps it kind of bend directly towards the bottom. Okay so that's cool now basically we've got two different ways that we can augment this basic SGD algorithm to hopefully improve its convergence and improve its stability in these weird situations. So now that we've got these two good ideas why not put them together so there's another very common optimization algorithm that you'll see used in deep learning a lot called Adam and Adam is basically RMS prop plus momentum so it's basically combining these two good ideas that we've seen into one optimization algorithm that uses both momentum as well as this adaptive learning rate's idea from Adagrad.

RMS prop你可以把它看作是Adagrad的泄漏版本。就像你记得在随机梯度下降加动量中，我们有某种摩擦系数，在计算之前每个迭代都会衰减速度。现在使用RMS prop，看起来很像给Adagrad添加了这个额外的摩擦项，它会衰减我们平方梯度的运行平均值。这里的希望是通过给我们的Adagrad算法添加某种摩擦，它将使其在训练过程中取得更好的进展，而不是一直减慢速度。回顾这个优化问题的相同示例很有启发性，现在比较黑色的SGD、蓝色的SGD加动量和红色的RMS prop。这里我们可以看到它们有相当抱歉的红橙色选择可能不是很好，但希望如果你有很好的色觉，你可能能够分辨出蓝色和红色在导航这个地形时有某种质的不同行为。当我们使用蓝色的基于动量的方法时，你可以看到它倾向于过冲然后返回，而当我们使用来自Adagrad或RMS prop的这种二阶自适应学习率思想时，你可以看到它在快速移动方向上抑制进展，同时在慢速移动方向上加速进展，这有助于它直接向底部弯曲。好的，这很酷，现在基本上我们有两种不同的方法来增强这个基本的SGD算法，希望能在这些奇怪情况下改善其收敛性和稳定性。既然我们有了这两个好主意，为什么不把它们结合起来呢，所以有另一个非常常见的优化算法，你会看到在深度学习中经常使用，叫做Adam，Adam基本上是RMS prop加动量，所以它基本上把我们看到的这两个好主意结合到一个优化算法中，既使用动量也使用来自Adagrad的自适应学习率思想。

So the exact formulation here is that we keep track of two things, two running quantities during optimization. One is the first moment, which is somewhat analogous to the velocity that we saw in SGD plus momentum, and the other is the second moment, which is the same as this leaky exponential average of squared gradients that we saw in RMS prop. Then when we make our final learning step, we end up doing both. So you can see here this part in red is kind of the momentum idea from SGD momentum, the blue part here is kind of this adaptive learning rates idea from RMS prop, and we basically put it together. Hopefully that gives us a really good optimization algorithm since we put two good ideas together. But there's a bit of, oh sorry, was there a question? Okay, so but there's maybe a slight, slightly subtle problem that could happen with Adam that also by the way applies to RMS prop. So that's the question of what might happen at the very beginning of optimization, especially thinking that what happens if our beta2 constant, which is our friction on the second moment, what if that value is some very large value like 0.999. Well now something very bad could happen at the very beginning of optimization because if you look at the algorithm again, we're initializing the second moment with zero, and that's then when we make this first gradient step. If our beta two is some value very close to one, then our second moment will still be very very close to zero at the very first step. So then when we take our very first gradient step, we'll be dividing by the square root of something very very close to zero. So that means that we could end up taking a very very large gradient step at the very beginning of optimization, and that could sometimes lead to very bad results. So the full form of Adam actually has a third good idea, which is to add a bit of bias correction to overcome this problem that might happen at the very beginning of optimization. So here the basic idea is that we want to make smaller steps at the very.

这里的精确表述是我们在优化过程中跟踪两个量：一个是第一矩，有点类似于我们在SGD加动量中看到的速度；另一个是第二矩，与我们在RMS prop中看到的平方梯度的泄漏指数平均值相同。然后当我们进行最终的学习步骤时，我们同时进行这两项操作。所以你可以看到这里的红色部分来自SGD动量的动量思想，蓝色部分来自RMS prop的自适应学习率思想，我们基本上将它们结合在一起。既然我们把两个好主意结合在一起，希望这能给我们一个非常好的优化算法。但是有一个，哦抱歉，刚才有问题吗？好的，但是Adam可能存在一个稍微微妙的问题，顺便说一下，这也适用于RMS prop。这就是在优化刚开始时可能发生什么的问题，特别是考虑如果我们的beta2常数（即我们对第二矩的摩擦）如果该值是一个非常大的值，比如0.999，会发生什么。那么现在在优化刚开始时可能会发生非常糟糕的事情，因为如果你再看一下算法，我们用零初始化第二矩，然后当我们进行第一个梯度步骤时，如果我们的beta2是一个非常接近一的值，那么我们的第二矩在第一步时仍然非常接近零。所以当我们进行第一个梯度步骤时，我们将除以非常接近零的东西的平方根。这意味着我们可能在优化刚开始时采取一个非常大的梯度步骤，这有时可能导致非常糟糕的结果。所以完整形式的Adam实际上有第三个好主意，即添加一些偏差校正来克服在优化刚开始时可能发生的这个问题。所以这里的基本思想是我们希望在刚开始时采取更小的步骤。

Beginning of optimization as we try to build up robust estimates of those first and second moments. So there's you can look at the paper for a full derivation of exactly why this form of bias correction is correct but the idea is just to overcome this problem of your moment estimates are biased towards zero at the beginning of optimization. So by the way this Adam algorithm actually works really well in practice for a lot of deep learning systems so this is definitely my go-to optimizer when I'm trying to build a new deep learning system. And as a bit of a pro tip if you use Adam with beta 1 is 0.9 beta 2 is 0.999 and learning rate is somewhere in the regime of 1e-3 or 1e-4 that actually seemed that surprisingly tends to work kind of out of the box on a very wide variety of different deep learning problems. And to kind of drive that point home I took some excerpts from some of my own recent papers where we showed like these five different papers that are using deep learning systems for very different types of systems but they're all using Adam. This one we did a bad job didn't record the learning rate that was bad. This one we were using Adam 1e-4 out of 1e-4 Adam 1e-3 Adam 1e-3. So these papers were all doing very different tasks but it turns out that Adam is a very robust optimization algorithm that tends to work across a wide variety of tasks with fairly minimal hyper parameter tuning so when you're designing your own network from scratch it's usually a pretty good go-to optimizer when you're first trying to get things off the ground. And now after hyping up Adam we've got to look at this diagram to see actually what it does and now because we remember we had this notion of Adam as trying to combine the good parts of momentum as well as these adaptive learning rates at a grad and when we look at this picture here we can somehow see that it has properties of both of those.

在优化开始时，我们试图建立对第一矩和第二矩的稳健估计。所以你可以查看论文以获得关于为什么这种偏差校正形式是正确的完整推导，但基本思想只是为了克服在优化开始时你的矩估计偏向零的问题。顺便说一下，这个Adam算法在实践中对于许多深度学习系统确实效果很好，所以当我要构建新的深度学习系统时，这绝对是我的首选优化器。作为一个小技巧，如果你使用Adam，beta1设为0.9，beta2设为0.999，学习率在1e-3或1e-4范围内，这实际上似乎出人意料地能在各种不同的深度学习问题上开箱即用。为了强调这一点，我摘录了我最近一些论文的内容，我们展示了这五篇不同的论文，它们使用深度学习系统处理非常不同类型的系统，但都使用了Adam。这篇我们做得不好，没有记录学习率，这很糟糕。这篇我们使用了Adam 1e-4，这篇是Adam 1e-3，这篇也是Adam 1e-3。所以这些论文都在处理非常不同的任务，但事实证明Adam是一个非常稳健的优化算法，能够在各种任务中工作，只需要最少量的超参数调整，所以当你从头开始设计自己的网络时，它通常是一个相当不错的首选优化器，特别是当你刚开始尝试让事情运转起来时。在大力宣传Adam之后，我们现在需要查看这个图表来了解它实际做了什么，因为我们记得Adam的概念是试图结合动量的优点以及Adagrad的自适应学习率，当我们看这张图时，我们可以在某种程度上看到它具有这两者的特性。

Optimization algorithms so we can see that somehow like the momentum based method it tends to build up some velocity and overshoot and come back but its overshoots are maybe less drastic than in the full then using this normal SGD plus momentum. And like RMS prop or Adagrad it tends to kind of bend in the right way towards the minimum and you can it tends to work well for a lot of problems for that reason. So again I kind of need to caution you again against making intuitions about high dimensional spaces based on low dimensional problems. So even when I'm putting all these pictures like this about how these optimization algorithms behave you really should take this with a huge shaker of salt because remember at the end of the day we're really training we're going to be training on very high dimensional spaces so the behavior in those very high dimensional spaces could look quite different than in these low dimensional projections that I'm showing you but I think it's still useful to look at these kinds of things to at least get a very coarse sense of intuition about what these algorithms are doing but try not to put too much faith in exactly what you see in these animations. Okay so as a bit of summary then we've seen this I put together this little comparison table of these different optimization algorithms that we've talked about and we can see that they kind of build up and iteratively add more features as we go along but this is maybe meant as a bit more review for you guys so far all these algorithms are what we call first order optimization algorithms because basically what they're doing is they're using information about the gradient and only the first derivative to make their gradient steps so what we can think about them as doing is basically they're forming a linear approximation to the function and then they form this linear approximation to the objective function we're trying to minimize which are using the gradient so then in a high dimensional situation you might imagine that we have this tangent hyperplane which is computed using the value of the gradient at the current point and then we step down on this tangent hyperplane.

优化算法我们可以看到，基于动量的方法会积累一些速度，会超调然后返回，但其超调可能没有完整使用这种普通SGD加动量那么剧烈。而像RMS prop或Adagrad，它倾向于以正确的方式向最小值弯曲，因此对于许多问题都能很好地工作。所以我需要再次提醒你们，不要基于低维问题来建立对高维空间的直觉。即使我展示所有这些关于优化算法行为的图片，你真的应该对此持保留态度，因为记住最终我们实际上是在非常高维的空间中进行训练，所以那些非常高维空间中的行为可能与我展示的这些低维投影看起来完全不同，但我认为看看这些东西仍然有用，至少能对这些算法在做什么有一个粗略的直觉，但不要过于相信你在这些动画中看到的确切内容。好的，作为一个小结，我们看到了这个，我整理了这个关于我们讨论过的不同优化算法的小比较表，我们可以看到它们逐步构建并迭代地添加更多特性，但这可能更多是给你们复习一下，到目前为止所有这些算法都是我们所说的一阶优化算法，因为它们基本上做的是使用梯度信息和仅一阶导数来进行梯度步骤，所以我们可以认为它们基本上是在形成函数的线性近似，然后形成我们试图最小化的目标函数的线性近似，这使用的是梯度，所以在高维情况下，你可能会想象我们有一个切超平面，它是使用当前点的梯度值计算的，然后我们沿着这个切超平面向下移动。

In order to try to minimize our objective function, we can easily naturally extend this thinking to use higher order gradient information. So in addition to using the gradient which is the first derivative, we might also form a quadratic approximation to our objective function using both the gradient as well as the Hessian which is the second derivative at every point. The idea here is that now we could form some quadratic surface which locally approximates our objective function in question and then step towards minimizing this quadratic approximation. This idea is nice because it lets the algorithm more adaptively choose how big it should step because maybe at a point like this where the gradient is rather high and the curvature is rather high, maybe we should take a fairly small step. But at a point over here, by looking at the second order curvature information, we can see that maybe it might be safer to take a larger step. So this idea of second order optimization is maybe more robust in setting learning rates or in setting step sizes for itself. Indeed, you can see algorithms like Adagrad and SGD momentum as some kind of diagonal approximation to these second order algorithms because they're trying to use by integrating first order information over time that's somehow forming some type of approximation to the second order curvature information, which again gives us a bit more intuition about why these momentum based methods or adaptive learning based methods might be a good idea. So then in a more formal math, we could write down the second order optimization by writing down this quadratic Taylor approximation to our function and then solving to write this w star value that was step to the minimum of the quadratic optimizer. But there's a problem is that this is actually not used so much in practice for a lot of deep learning systems. The problem here, can anyone spot it? Well the problem is that we want to work in high dimensional spaces right? So when we want to work, we want to be able to optimize models with tens of thousands or millions of parameters. Now the Hessian nature, the gradient is fine, the gradient.

为了最小化我们的目标函数，我们可以很自然地扩展这种思路来使用高阶梯度信息。除了使用作为一阶导数的梯度外，我们还可以使用梯度以及海森矩阵（即每点的二阶导数）来形成目标函数的二次近似。这里的想法是，现在我们可以形成某个二次曲面，局部近似我们讨论的目标函数，然后朝着最小化这个二次近似的方向移动。这个想法很好，因为它让算法能够更自适应地选择应该迈多大的步长，因为在梯度相当高、曲率也相当高的点，也许我们应该采取相当小的步长。但在这里的某个点，通过查看二阶曲率信息，我们可以看到也许采取更大的步长可能更安全。因此，这种二阶优化的思想在设置学习率或设置步长方面可能更加稳健。实际上，你可以将像Adagrad和SGD动量这样的算法视为对这些二阶算法的某种对角近似，因为它们试图通过随时间整合一阶信息，以某种方式形成对二阶曲率信息的某种近似，这再次让我们更直观地理解为什么这些基于动量的方法或基于自适应学习的方法可能是个好主意。那么，在更正式的数学中，我们可以通过写下函数的二次泰勒近似，然后求解得到这个w*值（即达到二次优化器最小值的步骤）来描述二阶优化。但问题是，这在实践中对于许多深度学习系统并不常用。这里的问题，有人能发现吗？问题在于我们想要在高维空间中工作，对吧？当我们想要工作时，我们希望能够优化具有数万或数百万个参数的模型。现在海森矩阵的性质，梯度没问题，梯度。

Has the same number of elements as the weight matrix itself, so if we can store and manipulate the weight matrix, we can also store and manipulate the gradient. But the problem is now the Hessian matrix is a matrix, so if our weight matrix has n values, then the second order Hessian matrix has n squared values. And now if we have like a hundred million parameters, 100 million squared is like way too big, you're not going to have enough memory. And now even worse is if you look at this equation, we actually need to invert the Hessian, and now inverting a general matrix is now cubic in the size of the matrix. So now inverting this thing is going to be like 100 million cubes, like heat death of the universe situation, like we don't want to go there. So in practice, these second order optimizers are sometimes used for low dimensional optimization problems, but they're not used so much in practice for very high dimensional optimization problems. Let's get that. So then in practice, when you're training your own models, Adam is a very nice default choice in a lot of situations, and so that's kind of a good go-to algorithm when you're trying to build a new deep learning system. Although I should also point out that SGD plus momentum is also used in practice quite a lot as well. So in practice, you'll typically see most deep learning papers use one of these two optimization algorithms. My general rule of thumb is to go with Adam at the very beginning because it's fairly easy to get to work and it tends not to require too much tuning of those hyperparameters. Those default values that I gave you some pro tips about tend to work out of the box for a lot of different problems, whereas SGD plus momentum can sometimes actually give better results but might require a bit more tuning of the hyperparameters. And in future lectures, we'll talk more about some of the strategies you might employ for tuning the hyperparameters of something like SGD momentum.

与权重矩阵本身具有相同数量的元素，所以如果我们能够存储和操作权重矩阵，我们也能够存储和操作梯度。但问题是海森矩阵是一个矩阵，所以如果我们的权重矩阵有n个值，那么二阶海森矩阵就有n的平方个值。如果我们有大约一亿个参数，一亿的平方就太大了，你将没有足够的内存。更糟糕的是，如果我们看这个方程，我们实际上需要求海森矩阵的逆，而求一般矩阵的逆在矩阵大小上是立方级的。所以求这个逆将需要大约一亿的立方次操作，就像宇宙热寂的情况一样，我们不想走到那一步。因此在实践中，这些二阶优化器有时用于低维优化问题，但在实践中不太常用于非常高维的优化问题。让我们理解这一点。那么在实践中，当你训练自己的模型时，Adam在很多情况下是一个非常不错的默认选择，所以当你尝试构建一个新的深度学习系统时，这是一个很好的首选算法。虽然我也应该指出，SGD加动量在实践中也经常使用。所以在实践中，你通常会看到大多数深度学习论文使用这两种优化算法之一。我的一般经验法则是从Adam开始，因为它相对容易上手，并且往往不需要过多调整那些超参数。我给你们的一些专业技巧的默认值往往在很多不同问题上都能开箱即用，而SGD加动量有时实际上能给出更好的结果，但可能需要更多超参数调整。在未来的讲座中，我们将更多地讨论你可能采用的一些策略，用于调整像SGD动量这样的超参数。

And if for some reason you can afford some kind of second order optimization, maybe your problem is low dimensional, maybe your problem is not stochastic because second order optimizers tend not to do well with stochasticity, then you might consider using some kind of second order optimizer. LBFGS is a good one for that. But in practice, the ones you'll usually see are Adam and SGD momentum. By this point now we've talked about how we can use linear models to solve image classification problems. Then in the last lecture we talked about how we can use loss functions to quantify how much we care about different values of the weight matrix in our linear models. And now in today's lecture we've seen how we can use stochastic gradient descent and its cousins to efficiently optimize these high dimensional loss surfaces. So now we've kind of equipped ourselves with a lot of different tools working up towards solving this deep learning problem.

如果你有条件使用某种二阶优化方法——也许你的问题是低维的，也许你的问题不是随机的，因为二阶优化器通常不太擅长处理随机性问题——那么你可以考虑使用某种二阶优化器。LBFGS在这方面是个不错的选择。但在实践中，你通常会看到的是Adam和SGD动量算法。到目前为止，我们已经讨论了如何使用线性模型解决图像分类问题。在上次讲座中，我们讨论了如何使用损失函数来量化我们对线性模型中权重矩阵不同值的关注程度。在今天这次讲座中，我们看到了如何使用随机梯度下降及其相关方法来高效优化这些高维损失曲面。因此，我们现在已经装备了许多不同的工具，为解决这个深度学习问题做好了准备。